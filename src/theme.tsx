// src/theme.js
import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      main: '#E22A7F',
    },
    secondary: {
      main: '#1A63CB',
    },
  },
});

export default theme;
